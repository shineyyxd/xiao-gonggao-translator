# 小公告翻译官（银发向）· 架构与版本演进

**版本**：v0.4 ｜ **更新**：2026-08-31 ｜ **代码库**：https://github.com/shineyyxd/xiao-gonggao-translator

> 一句话：把 A 股上市公司公告自动筛选、翻译成退休老人能听懂的大白话音频早报。
> 只讲事实，不做任何投资建议。

---

## 一、当前架构骨架图（v0.4 现状）

```
                      小公告翻译官 · 工程骨架（2026-08-31，v0.4）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  每天 07:29 定时（scheduler.py，本地常驻）
        │
        ▼
 ┌──────────────────────────────────────────────────────────────────┐
 │  Orchestrator（总编）— 确定性状态机，不用 LLM                        │
 │  场景路由：正常刊发 / 无货日简版 / 停刊告警（宁可停刊不可错发）/ 降级缓存 │
 │  每次运行发 run_id，全链路审计（audit jsonl + sqlite agent_calls）    │
 └──────────────────────────────────────────────────────────────────┘
        │
        ▼
 ① Fetcher（数据员 · 代码）
   巨潮资讯网公告 API（证监会指定披露平台，约3000条/日）
   重试3次 + 缓存降级 + 分页绕圈检测
   → 原始快照落盘 raw_announcements.json（任何一期可重放）
   → 完整性对账：接口声称总数 vs 实抓唯一数，覆盖率<90% 日报报警
        │
        ▼
 ② Editor（选稿编辑 · 规则，不用 LLM）
   三层漏斗：14 赛道分类（银行/公用/白酒/半导体/光伏/医药/有色…）
   → 重要性打分（分红10/业绩9/风险9/回购6/发债4/人事4/程序件1，银行+2）
   → 合规丢弃 → 多样性控制（板块≤3/事件≤2/最低分≥4/同公司同主题合并）
        │
        ▼
 ②.5 PDF 摘要层（pdf_summary.py，live 稿的"营养层"）
   入选公告抓 PDF 首页 → 句子级**抽取**摘要（只摘不改，≤500字）
   喂给 Writer —— 没它时稿子只能说"详见公告原文"
        │
        ▼
 ③ Memory（档案员 · 代码，不用 LLM）
   近7天已报道去重（重大风险豁免：要连续提醒）
   历史错误分布 → 只给 Checker 提示
   【铁律：只影响选什么/查什么，绝不进 Writer 上下文】
        │
        ▼
 ④ 写作质检闭环（每条稿单独走，打回重试≤2次，不过就枪毙）
   ┌──────────────────────────────────────────────────────┐
   │ Writer 子Agent（kimi-k3 · 思考档 low）                  │
   │  skill：prompts/生成Agent.md                            │
   │    模板三段：什么事/会怎样/出处                          │
   │    必解释词表（TLAC/回购注销/股权登记日…）                │
   │    三句式：到手算例/安心句/公司一句话介绍                  │
   │    中性事实框架防脑补（禁定性词，守合规红线）               │
   │  契约：只交 {line1,line2,line3} JSON，不符=打回           │
   │     ↓ 交稿                                              │
   │  Compliance 预检（代码）：数字逐一对原文/禁词/关键限定       │
   │     ↓ 通过                                              │
   │  Checker 子Agent（kimi-k3 · 思考档 max，独立配置）         │
   │  skill：prompts/校对Agent.md（A数字/B无中生有/             │
   │        C倾向话术/D遗漏限定 + 四件套白名单，只挑错不改稿）     │
   │     → 打回：带错误清单回 Writer 重写                      │
   └──────────────────────────────────────────────────────┘
   ※ 链接不经过任何 LLM，管线从数据字段直接注入（机制性消除 A 类错误）
        │
        ▼
 ⑤ Publisher（发行 · 代码）——双产线
   看：大字版 HTML（≥20px，链接折叠）+ 纯文字版 txt
   听：口播稿转换（broadcast.py：数字中文化保精度/绝不念链接长名英文）
       → Edge TTS（YunxiNeural 沉稳男声，-18% 语速）
       → 单条 mp3 + 整期合并 mp3
   （上线前 TTS 切火山引擎，约0.3-0.5元/天；公众号人工群发=人工终审）
        │
        ▼
 ┌──────────────────────────────────────────────────────────────────┐
 │  审计与评测底座（sqlite + 日报.md）                                  │
 │  error_log（A/B/C/D错误）· runs（准确率/一次通过率/prompt版本/        │
 │  数据完整性）· agent_calls（每角色每次调用：输入输出/耗时/token）      │
 │  reported_items（跨天去重）· 148项测试（含v1八处错误永久回归集）        │
 │  GitHub 版本管理（v0.3/v0.4 tag，扫雷零密钥泄漏）                    │
 └──────────────────────────────────────────────────────────────────┘

 设计哲学：LLM 只在④里那两台发动机，其余全是确定性代码——
 能审计、能重放、宁缺毋滥、只讲事实不做建议。
```

### 角色分工速查表

| 角色 | 类型 | 职责 |
|---|---|---|
| Orchestrator（总编） | 确定性状态机 | 调度、场景路由、全链路审计；不是 LLM |
| Fetcher（数据员） | 代码 | 巨潮抓取、重试降级、快照、完整性对账 |
| Editor（选稿编辑） | 规则代码 | 14 赛道三层漏斗 + 多样性控制 |
| Memory（档案员） | 代码 | 跨天去重、历史错误分布；绝不进 Writer 上下文 |
| Writer（撰稿） | **LLM**（kimi-k3·low） | 公告→大白话，铁律+词表+三句式+中性框架 |
| Checker（校对） | **LLM**（kimi-k3·max，独立配置） | 只挑错不改稿，A/B/C/D 四类检查 |
| Compliance（合规官） | 代码 | 数字原值/禁词/关键限定，机制性兜底 |
| Publisher（发行） | 代码 | 大字版/纯文字/口播稿/TTS 音频 |

---

## 二、版本演进史（四个大版本，每版都由真实反馈驱动）

```
v0.1  原型验证（08-26）——证明"这事能成"
  人工筛 130 条真实公告 + 同模型生成/校对
  ▸ 产出：首期测试报告，事实准确率 68%→100%
  ▸ 发现 3 个筛选规则缺陷 + 8 处生成错误 → 错误分类学 A/B/C/D
  ▸ 关键结论：两类错误可"机制性消除"（定下整个架构的调子）
      ↓ 驱动：测试报告暴露了错误模式

v0.2  工程化（08-26→27）——从手工到系统
  漏斗/生成/校对全部代码化 → 多Agent框架
  ▸ 8 角色分工（Orchestrator/Memory 确定性化，LLM 只管写校）
  ▸ 巨潮实时抓取、run_id 审计、回归集、14 赛道
  ▸ 测试 47→105 项
      ↓ 驱动：接真实 LLM 后连环踩坑

v0.3  真实 LLM 上岗（08-28）——Harness 加固
  Kimi 双子 Agent（k3-low 写 / k3-max 校）
  ▸ 踩坑实录：思考耗尽 max_tokens / 温度 400 / RPM=3 限速
    / 超时 / 402 余额耗尽 → 全部变成 Harness 规则
  ▸ token 计量入日报、停刊保护真实触发
  ▸ GitHub 建仓（tag: v0.3）
      ↓ 驱动：老人试测反馈（5 条稿只有 1 条全过）

v0.4  内容迭代 + 音频形态（08-30→31）——从"说对"到"听懂"
  ▸ 必解释词表 / 到手算例 / 安心句 / 中性事实框架（守红线）
  ▸ PDF 摘要（live 稿有真数字了）+ 完整性对账
  ▸ 口播稿规范 + TTS 音频（音频优先形态落地）
  ▸ 148 项测试全绿 → 真人听测通过（tag: v0.4）
```

### 逐版明细

| 版本 | 日期 | 主题 | 关键产出 | 驱动来源 |
|---|---|---|---|---|
| v0.1 | 08-26 | 原型验证 | 首期测试报告（68%→100%）、错误分类学、3 个规则缺陷修复 | 首期真实测试 |
| v0.2 | 08-26→27 | 工程化 | 多 Agent 框架、巨潮实时抓取、审计底座、回归集、14 赛道、105 项测试 | 手工流程不可复现 |
| v0.3 | 08-28 | 真实 LLM | Kimi 双子 Agent、Harness 四件套（温度/限速/超时/token 计量）、停刊保护实证 | 真实 API 连环踩坑 |
| v0.4 | 08-30→31 | 内容与形态 | 词表/算例/安心句/中性框架、PDF 摘要、完整性对账、口播音频、148 项测试、真人听测通过 | 老人试测 5 过 1 |

**演进规律**：每一版的改动都能指认"是谁的什么反馈逼出来的"——
测试报告逼出错误分类学，真实 LLM 逼出 Harness，老人逼出内容规则。
不是"v1 到 v4 加了功能"，是"v1 到 v4 消灭了四类真实问题"。

---

## 三、设计哲学（贯穿四个版本不变的三条）

1. **智能最小化**：8 个角色只有 Writer/Checker 用 LLM，调度、记忆、合规、审计全是确定性代码——所以可审计、可重放。
2. **机制性消除优先于模型能力**：能用规则消灭的错误（链接错、倾向话术）绝不靠模型自觉；每个 bad case 的终点是一行代码或一条测试。
3. **宁缺毋滥，只讲事实**：写不对就枪毙、抓不到就停刊；不做任何投资倾向判断——信任是这个产品的核心资产。
